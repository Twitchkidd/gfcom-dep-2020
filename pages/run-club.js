import { Layout } from '../components';

const RunClub = () => (
	<Layout>
		<h1>The `run-club` project page is a todo! Sorry!</h1>
		<h2>Please navigate back!</h2>
	</Layout>
);

export default RunClub;
